{
  inputs,
  outputs,
  stateVersion,
  username,
  colorscheme,
  terminal,
  browser,
  font,
  ...
}:
let
  helpers = import ./helpers.nix {
    inherit
      inputs
      outputs
      stateVersion
      username
      colorscheme
      terminal
      font
      browser
      ;
  };
in
{
  inherit (helpers)
    mkHome
    mkHost
    mkSystem
    forAllSystems
    ;
}
